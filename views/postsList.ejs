<!DOCTYPE html>
<html>
<head>
	<title>Blog Posts</title>

	<link rel="stylesheet" href="/stylesheets/style.css">
	<link rel="stylesheet" href="/stylesheets/postsList.css">
	<link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
</head>

<body>
	<% if (postsList.length === 0) { %>
		<p>Could not find blog posts written by the specified username. Please provide a request for a valid existing user.</p>
	<% } %>

	<% for (let i = 0; i < postsList.length; i++) { %>
		<div>
			<h1><%- postsList[i].title %></h1>

			<%- postsList[i].body %>

			<% if (i != postsList.length - 1) { %><hr><% } %>
		</div>
	<% } %>

	<% if (isMoreToRender) { %>
		<div id="nextBtn" class="text-center">
			<a href="<%= nextURL %>" id="next" class="btn btn-primary btn-block" role="button">Next</a>
		</div>
	<% } %>
</body>
</html>
